{
  if (mIsBeingFlung) {
    mIsBeingFlung=false;
    int x=getScrollX();
    int y=getScrollY();
    for (    GestureListener listener : mGestureListeners) {
      listener.onFlingComplete(x,y);
    }
  }
  invalidate();
}
