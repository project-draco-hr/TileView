{
  if (o instanceof Tile) {
    Tile m=(Tile)o;
    return (m.getRow() == getRow()) && (m.getCol() == getCol()) && (m.getWidth() == getWidth())&& (m.getHeight() == getHeight())&& (m.getData().equals(getData()));
  }
  return false;
}
