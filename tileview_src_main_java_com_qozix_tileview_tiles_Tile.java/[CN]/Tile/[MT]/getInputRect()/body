{
  if (mInputRect == null) {
    mInputRect=new Rect();
    mInputRect.top=0;
    mInputRect.left=0;
    mInputRect.bottom=mInputRect.top + getHeight();
    mInputRect.right=mInputRect.left + getWidth();
  }
  return mInputRect;
}
