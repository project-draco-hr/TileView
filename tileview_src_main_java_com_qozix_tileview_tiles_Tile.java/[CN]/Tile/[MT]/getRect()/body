{
  if (mRect == null) {
    mRect=new Rect();
    mRect.top=getTop();
    mRect.left=getLeft();
    mRect.bottom=mRect.top + getHeight();
    mRect.right=mRect.left + getWidth();
  }
  return mRect;
}
