{
  if (o == this) {
    return true;
  }
  if (o instanceof StateSnapshot) {
    StateSnapshot stateSnapshot=(StateSnapshot)o;
    return detailLevel.equals(stateSnapshot.detailLevel) && startRow == stateSnapshot.startRow && startColumn == stateSnapshot.startColumn && endRow == stateSnapshot.endRow && endColumn == stateSnapshot.endColumn;
  }
  return false;
}
