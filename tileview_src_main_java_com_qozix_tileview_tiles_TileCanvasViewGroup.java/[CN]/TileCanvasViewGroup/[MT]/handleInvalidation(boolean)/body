{
  if (shouldInvalidate) {
    mHasInvalidatedOnCleanOnce=false;
    invalidate();
  }
 else {
    if (!mHasInvalidatedOnCleanOnce) {
      mHasInvalidatedOnCleanOnce=true;
      invalidate();
    }
  }
}
