{
  Log.d("Tiles","TileManager.renderTiles");
  if (mRenderIsCancelled) {
    return;
  }
  if (mRenderIsSuppressed) {
    return;
  }
  if (mDetailLevelToRender == null) {
    return;
  }
  beginRenderTask();
}
