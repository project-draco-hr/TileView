{
  if (mMarkerTapListeners.isEmpty()) {
    return;
  }
  View view=getViewFromTap(point.x,point.y);
  if (view != null) {
    for (    MarkerTapListener listener : mMarkerTapListeners) {
      listener.onMarkerTap(view,point.x,point.y);
    }
  }
}
